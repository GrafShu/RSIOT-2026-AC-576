apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web28-app.fullname" . }}-test-connection"
  labels:
    {{- include "web28-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web28-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
